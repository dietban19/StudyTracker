export class Event {
  constructor(id, title, start, color, courseId, kind) {
    this.id = id;
    this.title = title;
    this.start = start;
    this.color = color;
    this.courseId = courseId;
    this.kind = kind;
  }
}
